const grid = document.querySelector("#products"); const toast = document.querySelector("#store-toast");
const money = (n) => `₹${Number(n || 0).toLocaleString("en-IN")}`;
const esc = (s) => String(s ?? "").replace(/[&<>'"]/g, (c) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", "'":"&#39;", '"':"&quot;" }[c]));
async function load() { const products = await fetch("/api/storefront/products").then(r => r.json()); grid.innerHTML = products.map((p) => `<article class="product-card"><div class="product-art"><span>${p.collection}</span><b>${p.name.split(" ").map(x=>x[0]).join("")}</b></div><div class="product-meta"><div class="eyebrow">${p.sku}</div><h2>${p.name}</h2><p>${p.collection} · QR traceable</p><div class="product-buy"><strong>${money(p.price)}</strong><button class="btn primary" data-buy="${p.id}">Reserve online</button></div></div></article>`).join(""); document.querySelectorAll("[data-buy]").forEach((button) => button.addEventListener("click", () => checkout(button.dataset.buy, products.find(p => p.id === button.dataset.buy)))); }
function checkout(productId, product) { document.body.insertAdjacentHTML("beforeend", `<div class="modal-backdrop" id="store-modal"><div class="modal"><div class="modal-head"><div><div class="eyebrow">ONLINE ORDER</div><div class="panel-title">Reserve ${esc(product.name)}</div></div><button class="close" data-close>×</button></div><form id="store-form"><div class="form-grid"><div class="field full"><label>Your name</label><input name="customerName" required /></div><div class="field"><label>Email</label><input type="email" name="email" required /></div><div class="field"><label>City</label><input name="city" required /></div><div class="field"><label>Quantity</label><input type="number" name="qty" min="1" max="${product.available}" value="1" required /></div><div class="field"><label>Payment</label><select name="paymentMode"><option>Prepaid</option><option>COD</option></select></div><div class="field full"><label><input type="checkbox" name="marketingConsent" value="true" /> I agree to receive collection updates on email/WhatsApp.</label></div></div><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:20px"><button type="button" class="btn" data-close>Cancel</button><button class="btn primary">Confirm reservation</button></div></form></div></div>`); document.querySelectorAll("[data-close]").forEach(e => e.addEventListener("click", () => document.querySelector("#store-modal")?.remove())); document.querySelector("#store-form").addEventListener("submit", async (e) => { e.preventDefault(); const payload = Object.fromEntries(new FormData(e.currentTarget)); payload.productId = productId; payload.qty = Number(payload.qty); payload.marketingConsent = payload.marketingConsent === "true"; const response = await fetch("/api/storefront/orders", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify(payload) }); const result = await response.json(); if (!response.ok) return showToast(result.error); document.querySelector("#store-modal").remove(); showToast(`Reserved ${result.orderId}. We’ll confirm delivery shortly.`); }); }
function showToast(message) { toast.textContent = message; toast.classList.add("show"); setTimeout(() => toast.classList.remove("show"), 4000); }
load().catch(() => { grid.innerHTML = `<div class="empty">The collection is temporarily unavailable.</div>`; });
